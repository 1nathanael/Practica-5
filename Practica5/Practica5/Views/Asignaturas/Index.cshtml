@model IEnumerable<Practica5.Models.Asignaturas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreAS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreAS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Asignatura_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Asignatura_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Asignatura_id })
        </td>
    </tr>
}

</table>
